# Production (EC2) — on the server:
#   cp .env.production.example .env
#   docker compose up -d --force-recreate keycloak serve web
#
# Update PUBLIC_* if the Elastic IP / domain changes.

BACKCAT_ENV=production
PUBLIC_WEB=http://34.228.39.244:3000
PUBLIC_KEYCLOAK=http://34.228.39.244:8080
PUBLIC_SERVE=http://34.228.39.244:8000
INTERNAL_TOKEN=dev-internal-token
# BYOK_SECRET=   # optional; set a Fernet key in prod
