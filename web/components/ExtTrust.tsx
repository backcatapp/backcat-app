export default function ExtTrust() {
  return (
    <section className="ext-section" data-reveal>
      <h2 className="display ext-section-title">Grounded, or honest absence</h2>
      <p className="ext-section-sub" style={{ maxWidth: 520 }}>
        Every factual claim cites a retrieved chunk. If the catalog doesn&apos;t cover
        it, we say so — we never fake coverage.
      </p>
    </section>
  );
}
