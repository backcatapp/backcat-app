# Drop product screenshots here (PNG/WebP). Filenames expected by the landing:
#   hero-sidepanel.png  — full hero (browser chrome + side panel)
#   shot-ask.png        — Ask + citations
#   shot-graph.png      — concept chips / graph
#   shot-channels.png   — channels / index list
#
# Until files exist, dashed placeholder frames show on the page.
